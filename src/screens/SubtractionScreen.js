import React from 'react';
import {Text, View} from 'react-native';

const Subtraction = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>Subtration Screen</Text>
    </View>
  );
};

export default Subtraction;
