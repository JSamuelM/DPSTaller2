import React from 'react';
import {Text, View} from 'react-native';

const Division = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>Division Screen</Text>
    </View>
  );
};

export default Division;
